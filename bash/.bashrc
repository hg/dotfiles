# .bashrc

[[ -f /etc/bashrc ]] && source /etc/bashrc
[[ -f ~/.bash_aliases ]] && source ~/.bash_aliases

shopt -s autocd

export HISTTIMEFORMAT='%Y-%m-%d %H:%M:%S  '
export HISTFILESIZE=
export HISTSIZE=

