general {
	interval = 5
	colors = true
}

order += "ethernet wg0"
order += "ethernet tun0"
order += "volume master"
order += "tztime date"
order += "tztime time"

ethernet wg0 {
	format_up = "wg: %ip"
	format_down = "wg: no"
}

ethernet tun0 {
	format_up = "new: %ip"
	format_down = "new: no"
}

volume master {
	format = "♪: %volume"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}

tztime date {
	format = "%A, %B %d"
}

tztime time {
	format = "%H:%M:%S"
}

