general {
	interval = 5
	colors = true
}

order += "ethernet wg0"
order += "volume master"
order += "tztime date"
order += "tztime time"

ethernet wg0 {
	format_up = "wg: %ip"
	format_down = "wg: no"
}

volume master {
	format = "♪: %volume"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}

tztime date {
	format = "%A, %B %d"
}

tztime time {
	format = "%H:%M:%S"
}

