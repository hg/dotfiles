general {
	interval = 5
	colors = true
}

order += "volume master"
order += "tztime date"
order += "tztime time"

volume master {
	format = "♪: %volume"
	device = "default"
	mixer = "Master"
	mixer_idx = 0
}

tztime date {
	format = "%B, %A %d"
}

tztime time {
	format = "%H:%M:%S"
}

